model Task {
  id          String   @id @default(uuid())
  projectId   String
  assignTo    String[]
  title       String
  description String?
  startTime   DateTime
  endTime     DateTime
  location    String?
  attachment  String?
  labels      String[]

  assignees User[]
  project   Project @relation(fields: [projectId], references: [id])
}
